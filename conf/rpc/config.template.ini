zk=${ZK_HOST}

product=${ZK_PRODUCT}
verbose=${ZK_VERBOSE}

zk_session_timeout=${ZK_SESSION_TIME_OUT}

service=${RPC_SERVICE_NAME}
front_port=${LB_FRONT_PORT}
back_address=${LB_BACK_ADDRESS}

ip_prefix=${LB_IP_PREFIX}
proxy_address=${RPC_PROXY_ADDRESS}
worker_pool_size=${WORK_POOL_SIZE}